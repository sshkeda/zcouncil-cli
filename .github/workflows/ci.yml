name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [22, 24]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
      - name: Verify --help runs
        run: node bridge.mjs --help
      - name: Verify --version runs
        run: node bridge.mjs --version
      - name: Verify missing-token error path
        run: |
          tmp_home="$(mktemp -d)"
          set +e
          HOME="$tmp_home" node bridge.mjs > out.txt 2> err.txt
          code=$?
          set -e
          rm -rf "$tmp_home"
          test "$code" = "2" || (echo "expected exit 2, got $code" && cat err.txt && exit 1)
          grep -q "No token entered" err.txt
